<%= render 'components/banner' %>
<div class="container">
  <h3 class="browse-txt">You can track and share your child's medical events super easy now!</h3>
  <div class="row episodes-grid">
    <% @episodes.each do |episode| %>
      <div class="col-xs-12 col-md-4 col-lg-4 col-xl-3">
        <%= render 'components/democard', episode: episode %>
      </div>
    <% end %>
  </div>
</div>
